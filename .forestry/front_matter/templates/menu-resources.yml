---
label: Menu - Resources
hide_body: true
fields:
- name: items
  type: field_group_list
  fields:
  - name: include
    type: select
    default: []
    config:
      required: false
      options: []
      source:
        type: simple
        section: 
        file: 
        path: 
    label: Include
  config:
    min: 1
    max: 4
    labelField: include
  label: Menu items
- name: blocks
  type: blocks
  label: blocks
  template_types:
  - menu-resources
  - menu-resources
  config:
    min: 
    max: 
- name: list
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: pages
  label: list
