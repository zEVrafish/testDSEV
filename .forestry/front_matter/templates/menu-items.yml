---
label: Menu items
hide_body: true
fields:
- name: test
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: Text
- name: test1
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: documents
      section: pages
      file: 
  label: document fields
